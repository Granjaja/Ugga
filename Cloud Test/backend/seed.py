# def seed_admin(db: ):
#     admin_email = User(
#         email=os.getenv("ADMIN_EMAIL"),
    
#     admin = db.query(User).filter(User.email == admin_email.email).first()

    
#     db.add(admin)
#     db.commit()
#     )